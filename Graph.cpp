//
// Created by Francisca Fernandes on 17/12/2023.
//

#include "Graph.h"

//Construtor
//DFS







/*
void Graph::addAirport(const Airport& airport) {
    // Inicializa uma lista vazia para o aeroporto se ainda não existir uma.
    if (adjList.find(airport.getCode()) == adjList.end()) {
        adjList[airport.getCode()] = std::vector<std::string>();
    }
}


void Graph::addConnection(const std::string& fromCode, const std::string& toCode) {
    adjList[fromCode].push_back(toCode);
}
 */

